# ============================================
# DATABASE CONFIGURATION
# ============================================
# Option 1: Use DATABASE_URL directly (from Replit)
DATABASE_URL=postgresql://user:password@host:port/teacher_buddy

# Option 2: Use individual credentials (for local Docker setup)
# Comment out DATABASE_URL above and uncomment these:
# POSTGRES_DB=teacher_buddy
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=your_secure_password_here
# DB_PORT=5432

# ============================================
# APPLICATION CONFIGURATION
# ============================================
NODE_ENV=production
PORT=5000
APP_PORT=5000

# ============================================
# AUTHENTICATION CONFIGURATION
# ============================================
# SESSION_SECRET: Generate with: openssl rand -hex 32
SESSION_SECRET=your_session_secret_here

# REPL_ID: Only needed if running on Replit platform
# Leave empty or set to "local-docker" for Docker deployment
REPL_ID=local-docker

# ISSUER_URL: OpenID provider
ISSUER_URL=https://replit.com/oidc
